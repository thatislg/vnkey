[package]
name = "vnkey-engine"
version = "1.0.1"
edition = "2021"
description = "Cross-platform Vietnamese input engine (VnKey)"
license = "GPL-3.0-or-later"

[lib]
name = "vnkey_engine"
crate-type = ["lib", "staticlib"]

[dependencies]
once_cell = "1"
